
        
        int historyCompare = strcmp(args[0], "!!");
        if(historyCompare == 0) {
            execvp(history[0], history);
        } else {
            for(int i = 0; i < sizeof(args); i++) {
              history[i] = args[i];
            }
            execvp(args[0], args);
        }